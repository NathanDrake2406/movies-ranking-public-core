/**
 * Canonical score version for persisted movie scores and version-scoped indexes.
 * Bump only when weights or scoring behavior changes and coordinate migration/index updates.
 */
export const CURRENT_SCORE_VERSION = 2;
